{
  "name": "bid-intelligence-os",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"npm run dev -w apps/web\" \"npm run dev -w apps/api\"",
    "dev:web": "npm run dev -w apps/web",
    "dev:api": "npm run dev -w apps/api",
    "build": "npm run build -w apps/web",
    "start": "NODE_ENV=production npm run start -w apps/api",
    "typecheck": "npm run typecheck -w apps/web && npm run typecheck -w apps/api",
    "check:security": "node scripts/check-cca-core-browser-leak.mjs && npm run test:security -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
