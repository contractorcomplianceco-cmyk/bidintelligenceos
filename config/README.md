# Config

Environment templates for each app. Copy the values you need into a `.env`
file at the repo root (the root `.env.example` is the canonical template —
`npm run dev` picks it up for both apps).

- `web.env.example` — frontend flags (demo mode)
- `api.env.example` — API scaffold settings (port)

No secrets are required to run this project.
