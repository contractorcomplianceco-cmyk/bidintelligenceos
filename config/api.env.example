# Port for the API server (production listens here after `npm run start`).
API_PORT=5001
HOST=0.0.0.0
NODE_ENV=development

# Optional CORS when web and API run on different hosts.
# CORS_ORIGIN=http://localhost:5173
